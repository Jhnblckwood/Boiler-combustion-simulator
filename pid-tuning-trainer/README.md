# PID Tuning Trainer (Standalone)

This project is intentionally separated from the combustion simulator implementation.

## Run
Open `index.html` in a browser.

## Notes
- Pure HTML/CSS/JavaScript (no build step).
- Includes PID controls (`Kp`, `Ki`, `Kd`), process model tuning (`K`, `τ`, dead time, noise), and live plotting.
